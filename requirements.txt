fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
httpx>=0.27.0          # for async API testing with httpx.AsyncClient
pytest>=8.1.0
pytest-asyncio>=0.23.0

# ML pipeline (install separately when training — TF is large)
# tensorflow>=2.16.0
# numpy>=1.26.0
